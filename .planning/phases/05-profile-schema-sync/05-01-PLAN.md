---
phase: 05-profile-schema-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/finyx/profile.md
  - commands/finyx/update.md
autonomous: true
gap_closure: true
requirements:
  - PROF-04
  - PROF-05
  - INVEST-01
  - INVEST-04
  - PENSION-01
  - PENSION-06

must_haves:
  truths:
    - "Profile created by /finyx:profile includes brokers[], _holdings_schema, and pension block"
    - "Completion banner lists all 4 specialist commands (tax, invest, broker, pension)"
    - "update.md explicitly appends legal disclaimer to output"
  artifacts:
    - path: "commands/finyx/profile.md"
      provides: "Profile interview command with synced schema and accurate banner"
      contains: "_holdings_schema"
    - path: "commands/finyx/update.md"
      provides: "Update command with disclaimer append instruction"
      contains: "Append the legal disclaimer"
  key_links:
    - from: "commands/finyx/profile.md (Phase 5 JSON)"
      to: "finyx/templates/profile.json"
      via: "identical schema structure"
      pattern: "_holdings_schema|brokers.*\\[\\]|pension"
    - from: "commands/finyx/update.md"
      to: "finyx/references/disclaimer.md"
      via: "execution_context load + explicit append instruction"
      pattern: "Append the legal disclaimer"
---

<objective>

Close 3 v1.0 audit gaps: sync profile.md's embedded JSON schema with finyx/templates/profile.json, fix the stale completion banner, and add the missing disclaimer append instruction in update.md.

Purpose: Profiles created by /finyx:profile currently lack brokers[], _holdings_schema, and pension fields — breaking downstream commands (invest, broker, pension). The banner misleads users about available commands.
Output: Two corrected command files.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

@finyx/templates/profile.json
@commands/finyx/profile.md
@commands/finyx/update.md

<interfaces>
<!-- The canonical schema is finyx/templates/profile.json. profile.md's Phase 5 JSON must match it exactly. -->
<!-- Key fields MISSING from profile.md but present in profile.json: -->

Top-level key (after "updated"):
```json
"_holdings_schema": {
  "description": "Each broker in countries.{country}.brokers[] can contain a holdings[] array",
  "holding_fields": {
    "ticker": "Exchange ticker symbol (e.g., VWCE, BOVA11)",
    "isin": "ISIN if available (e.g., IE00BK5BQT80), null for B3 assets",
    "shares": "Number of shares/units held (decimal)",
    "cost_basis": "Average cost per share in local currency",
    "asset_class": "One of: equity-etf, bond-etf, reit-etf, stock, fii, fixed-income",
    "geography": "One of: global, us, europe, emerging-markets, brazil, germany"
  }
}
```

In countries.germany (after marginal_rate):
```json
"brokers": []
```

In countries.brazil (after cpf):
```json
"brokers": []
```

Top-level key (after goals):
```json
"pension": {
  "de_rentenpunkte": null,
  "expected_real_return_de": 1.5,
  "br_inss_status": null,
  "expected_real_return_br": 2.0,
  "target_retirement_age": null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync profile.md Phase 5 JSON schema and fix Phase 6 banner</name>
  <files>commands/finyx/profile.md</files>
  <read_first>
    - finyx/templates/profile.json (canonical schema — the target)
    - commands/finyx/profile.md (current state — lines 267-375 Phase 5, lines 434-485 Phase 6)
  </read_first>
  <action>
Edit commands/finyx/profile.md with these 3 changes:

**Change 1 — Add _holdings_schema block (after line 286 "updated" field, before "identity"):**

Insert this block between the `"updated"` line and the `"identity"` block:

```json
  "_holdings_schema": {
    "description": "Each broker in countries.{country}.brokers[] can contain a holdings[] array",
    "holding_fields": {
      "ticker": "Exchange ticker symbol (e.g., VWCE, BOVA11)",
      "isin": "ISIN if available (e.g., IE00BK5BQT80), null for B3 assets",
      "shares": "Number of shares/units held (decimal)",
      "cost_basis": "Average cost per share in local currency",
      "asset_class": "One of: equity-etf, bond-etf, reit-etf, stock, fii, fixed-income",
      "geography": "One of: global, us, europe, emerging-markets, brazil, germany"
    }
  },
```

**Change 2 — Add brokers[] to both country blocks:**

In the `countries.germany` object (currently ends at `"marginal_rate"`), add after marginal_rate:
```json
      "brokers": []
```

In the `countries.brazil` object (currently ends at `"cpf"`), add after cpf:
```json
      "brokers": []
```

**Change 3 — Add pension block (after "goals" block, before "project" block):**

Insert between the closing `}` of `"goals"` and `"project"`:
```json
  "pension": {
    "de_rentenpunkte": null,
    "expected_real_return_de": 1.5,
    "br_inss_status": null,
    "expected_real_return_br": 2.0,
    "target_retirement_age": null
  },
```

**Change 4 — Fix Phase 6 completion banner (around line 481-485):**

Replace these lines:
```
Other advisors (coming in future releases):
  /finyx:tax     — Tax optimization advisor
  /finyx:invest  — Investment portfolio advisor
  /finyx:pension — Pension planning advisor
```

With:
```
Financial advisors (ready to use):
  /finyx:tax     — Tax optimization advisor
  /finyx:invest  — Investment portfolio advisor
  /finyx:broker  — Broker comparison advisor
  /finyx:pension — Pension planning advisor

Run /finyx:help for all available commands.
```

Note: Add `/finyx:broker` which was missing entirely from the old banner.
  </action>
  <verify>
    <automated>cd /Users/italovietro/projects/immo && grep -c "_holdings_schema" commands/finyx/profile.md && grep -c '"brokers": \[\]' commands/finyx/profile.md && grep -c '"pension"' commands/finyx/profile.md && grep -c "ready to use" commands/finyx/profile.md && grep -c "finyx:broker" commands/finyx/profile.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "_holdings_schema" commands/finyx/profile.md returns match
    - grep '"brokers": \[\]' commands/finyx/profile.md returns 2 matches (DE + BR)
    - grep "de_rentenpunkte" commands/finyx/profile.md returns match
    - grep "br_inss_status" commands/finyx/profile.md returns match
    - grep "target_retirement_age" commands/finyx/profile.md returns match
    - grep "coming in future releases" commands/finyx/profile.md returns NO matches
    - grep "ready to use" commands/finyx/profile.md returns match
    - grep "finyx:broker" commands/finyx/profile.md returns match
  </acceptance_criteria>
  <done>Profile.md Phase 5 JSON matches finyx/templates/profile.json (all 4 missing blocks added). Phase 6 banner lists all 4 specialist commands as available. No "coming in future releases" text remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit disclaimer append instruction to update.md</name>
  <files>commands/finyx/update.md</files>
  <read_first>
    - commands/finyx/update.md (full file — it's short, ~113 lines)
    - finyx/references/disclaimer.md (the disclaimer content)
  </read_first>
  <action>
Edit commands/finyx/update.md to add an explicit disclaimer append instruction.

At the end of Step 6 (after the closing ``` of the display banner, around line 102), before the `</process>` tag, add:

```markdown

## Step 7: Disclaimer

Append the legal disclaimer from the loaded disclaimer.md reference at the end of this output.
```

This mirrors the pattern used in profile.md (line 487) and other advisory commands.
  </action>
  <verify>
    <automated>cd /Users/italovietro/projects/immo && grep -c "Append the legal disclaimer" commands/finyx/update.md && grep -c "Step 7" commands/finyx/update.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Append the legal disclaimer" commands/finyx/update.md returns 1 match
    - grep "Step 7: Disclaimer" commands/finyx/update.md returns 1 match
    - The instruction appears inside the process block (before closing process tag)
  </acceptance_criteria>
  <done>update.md has an explicit "Append the legal disclaimer" instruction in a new Step 7, ensuring PROF-05 compliance.</done>
</task>

</tasks>

<verification>

After both tasks complete, verify all 3 audit gaps are closed:

```bash
# MISS-01: Schema synced — check all 4 missing blocks exist
grep "_holdings_schema" commands/finyx/profile.md
grep '"brokers": \[\]' commands/finyx/profile.md | wc -l  # expect 2
grep "de_rentenpunkte" commands/finyx/profile.md
grep "target_retirement_age" commands/finyx/profile.md

# MISS-02: Banner fixed — no stale text, all commands listed
grep -c "coming in future releases" commands/finyx/profile.md  # expect 0
grep "finyx:broker" commands/finyx/profile.md
grep "ready to use" commands/finyx/profile.md

# MISS-03: Disclaimer append instruction exists
grep "Append the legal disclaimer" commands/finyx/update.md
```

</verification>

<success_criteria>

1. `commands/finyx/profile.md` Phase 5 JSON template contains _holdings_schema, brokers[] in both country blocks, and pension block — matching finyx/templates/profile.json structure
2. Phase 6 completion banner lists /finyx:tax, /finyx:invest, /finyx:broker, /finyx:pension as available (not "coming in future releases")
3. `commands/finyx/update.md` contains explicit "Append the legal disclaimer" instruction in process body
4. No other files modified

</success_criteria>

<output>
After completion, create `.planning/phases/05-profile-schema-sync/05-01-SUMMARY.md`
</output>
