---
phase: 16-bulk-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/pension/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-04, SKILL-05]

must_haves:
  truths:
    - "/finyx:pension produces pension advisory output"
    - "Skill has disable-model-invocation: true"
    - "All paths use ${CLAUDE_SKILL_DIR}/references/"
  artifacts:
    - path: "skills/pension/SKILL.md"
      provides: "Full pension advisory skill"
      min_lines: 300
  key_links:
    - from: "skills/pension/SKILL.md"
      to: "skills/pension/references/"
      via: "${CLAUDE_SKILL_DIR}/references/ paths"
      pattern: "CLAUDE_SKILL_DIR.*references"
---

<objective>
Convert pension.md into skills/pension/SKILL.md following the validated conversion checklist.

Purpose: Pension planning advisory skill
Output: Full SKILL.md replacing the stub
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-bulk-migration/16-CONTEXT.md
@.planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
@skills/tax/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert pension command to pension SKILL.md</name>
  <read_first>
    - commands/finyx/pension.md
    - skills/pension/SKILL.md (current stub)
    - skills/tax/SKILL.md (reference pattern)
    - .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
    - skills/pension/references/ (list contents)
  </read_first>
  <files>skills/pension/SKILL.md</files>
  <action>
Overwrite the pension SKILL.md stub with full skill content:

1. Frontmatter:
   - name: finyx-pension
   - description: Under 250 chars, front-loaded. Start with "Pension planning advisor" — cover Riester, Rürup, betriebliche Altersvorsorge, INSS, PGBL/VGBL, retirement gap analysis
   - allowed-tools: Copy from pension.md. Remove Task if not needed
   - disable-model-invocation: true (advisory skill)

2. Content: Direct 1:1 conversion from pension.md
   - Copy objective, execution_context, process, error_handling, notes sections
   - Update all paths to ${CLAUDE_SKILL_DIR}/references/
   - References available: disclaimer.md, germany/pension.md, brazil/pension.md

3. Path handling:
   - All reference paths: ${CLAUDE_SKILL_DIR}/references/
   - @.finyx/profile.json kept as-is
   - Zero @~/.claude/ occurrences

4. Cross-skill references: Add fallback notes for any tax/insurance references
  </action>
  <verify>
    <automated>bash -c 'F="skills/pension/SKILL.md"; grep -q "disable-model-invocation: true" "$F" && test $(grep -c "@~/.claude/" "$F") -eq 0 && test $(wc -l < "$F") -gt 300 && grep -q "finyx-pension" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - skills/pension/SKILL.md > 300 lines
    - Contains `disable-model-invocation: true`
    - Contains `name: finyx-pension`
    - Zero occurrences of `@~/.claude/`
    - Key domain terms present: Riester or Rürup or pension gap
  </acceptance_criteria>
  <done>pension SKILL.md is a complete skill with proper frontmatter and portable paths</done>
</task>

<task type="auto">
  <name>Task 2: Validate pension skill completeness</name>
  <read_first>
    - skills/pension/SKILL.md (just written)
  </read_first>
  <files>skills/pension/SKILL.md</files>
  <action>
Run validation checks per CONVERSION-CHECKLIST.md:

1. grep -q "disable-model-invocation: true" skills/pension/SKILL.md
2. grep -c "@~/.claude/" (must be 0)
3. wc -l (must be > 300)
4. Verify ${CLAUDE_SKILL_DIR}/references/ paths resolve to files in skills/pension/references/
5. Grep for key domain terms: "pension", "retirement", "Riester" or "Rürup" or "INSS"

Fix any issues found.
  </action>
  <verify>
    <automated>bash -c 'F="skills/pension/SKILL.md"; grep -qi "pension" "$F" && grep -qi "retire\|retirement\|Rente" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - All validation checks pass
    - Key domain terms present
  </acceptance_criteria>
  <done>pension skill validated against checklist</done>
</task>

</tasks>

<verification>
- skills/pension/SKILL.md exists with > 300 lines
- disable-model-invocation: true present
- Zero @~/.claude/ paths
- Domain terms present
</verification>

<success_criteria>
pension SKILL.md is a complete, validated skill with proper plugin frontmatter and portable paths.
</success_criteria>

<output>
After completion, create `.planning/phases/16-bulk-migration/16-03-SUMMARY.md`
</output>
