---
phase: 16-bulk-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/invest/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-04, SKILL-05]

must_haves:
  truths:
    - "/finyx:invest produces investment advisory output with broker guidance merged in"
    - "Skill has disable-model-invocation: true"
    - "All paths use ${CLAUDE_SKILL_DIR}/references/"
  artifacts:
    - path: "skills/invest/SKILL.md"
      provides: "Full investment + broker advisory skill"
      min_lines: 400
  key_links:
    - from: "skills/invest/SKILL.md"
      to: "skills/invest/references/"
      via: "${CLAUDE_SKILL_DIR}/references/ paths"
      pattern: "CLAUDE_SKILL_DIR.*references"
---

<objective>
Convert invest.md + broker.md into skills/invest/SKILL.md following the validated conversion checklist.

Purpose: Merge investment advisory and broker comparison into a single skill
Output: Full SKILL.md replacing the stub
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-bulk-migration/16-CONTEXT.md
@.planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
@skills/tax/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert invest + broker commands to invest SKILL.md</name>
  <read_first>
    - commands/finyx/invest.md
    - commands/finyx/broker.md
    - skills/invest/SKILL.md (current stub)
    - skills/tax/SKILL.md (reference pattern)
    - .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
    - skills/invest/references/ (list contents to verify paths)
  </read_first>
  <files>skills/invest/SKILL.md</files>
  <action>
Overwrite the invest SKILL.md stub with full skill content:

1. Frontmatter:
   - name: finyx-invest
   - description: Under 250 chars, front-loaded. Start with "Investment portfolio advisor" — cover ETF analysis, asset allocation, broker comparison, rebalancing, DCA vs lump sum
   - allowed-tools: Copy from invest.md. If broker.md has additional tools, merge them. Remove Task if no agent delegation
   - disable-model-invocation: true (advisory skill per checklist)

2. Content merge strategy:
   - invest.md is the primary — its objective, process phases, error_handling, notes become the skeleton
   - broker.md content gets merged as additional phases or subsections within the invest skill
   - Add a "Broker Comparison" phase that absorbs broker.md's process section
   - Keep all existing invest.md phases intact

3. Path handling:
   - All reference paths must use ${CLAUDE_SKILL_DIR}/references/ format
   - References available: disclaimer.md, germany/brokers.md, brazil/brokers.md
   - @.finyx/profile.json kept as-is (runtime file)
   - Zero occurrences of @~/.claude/

4. Cross-skill references:
   - If invest.md references tax or insurance docs, add fallback note per checklist pattern
  </action>
  <verify>
    <automated>bash -c 'F="skills/invest/SKILL.md"; grep -q "disable-model-invocation: true" "$F" && test $(grep -c "@~/.claude/" "$F") -eq 0 && test $(wc -l < "$F") -gt 400 && grep -q "finyx-invest" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - skills/invest/SKILL.md > 400 lines
    - Contains `disable-model-invocation: true`
    - Contains `name: finyx-invest`
    - Zero occurrences of `@~/.claude/`
    - Contains broker comparison content (grep for "broker" or "Broker")
    - All ${CLAUDE_SKILL_DIR}/references/ paths resolve to files in skills/invest/references/
  </acceptance_criteria>
  <done>invest SKILL.md is a complete skill with invest + broker content merged, proper frontmatter, portable paths</done>
</task>

<task type="auto">
  <name>Task 2: Validate invest skill completeness</name>
  <read_first>
    - skills/invest/SKILL.md (just written)
  </read_first>
  <files>skills/invest/SKILL.md</files>
  <action>
Run validation checks per CONVERSION-CHECKLIST.md:

1. grep -q "disable-model-invocation: true" skills/invest/SKILL.md
2. grep -c "@~/.claude/" skills/invest/SKILL.md (must be 0)
3. wc -l skills/invest/SKILL.md (must be > 400)
4. Verify all ${CLAUDE_SKILL_DIR}/references/ paths have matching files:
   - Extract each path, check file exists under skills/invest/references/
5. Grep for key domain terms: "ETF", "rebalancing", "broker", "allocation"
6. Verify frontmatter has all required fields

Fix any issues found.
  </action>
  <verify>
    <automated>bash -c 'F="skills/invest/SKILL.md"; grep -q "ETF\|etf" "$F" && grep -q "broker\|Broker" "$F" && grep -q "allocation\|Allocation" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - All validation checks pass
    - Key domain terms present: ETF, broker, allocation
  </acceptance_criteria>
  <done>invest skill validated against checklist — all checks green</done>
</task>

</tasks>

<verification>
- skills/invest/SKILL.md exists with > 400 lines
- disable-model-invocation: true present
- Zero @~/.claude/ paths
- Broker content merged (not lost)
- Domain terms present
</verification>

<success_criteria>
invest SKILL.md is a complete, validated skill combining invest + broker advisory content with proper plugin frontmatter and portable paths.
</success_criteria>

<output>
After completion, create `.planning/phases/16-bulk-migration/16-01-SUMMARY.md`
</output>
