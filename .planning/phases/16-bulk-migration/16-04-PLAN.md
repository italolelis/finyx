---
phase: 16-bulk-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/insurance/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, SKILL-04, SKILL-05]

must_haves:
  truths:
    - "/finyx:insurance produces health insurance advisory with calc and research agents"
    - "Skill has disable-model-invocation: true"
    - "Both agents scoped under skills/insurance/agents/"
    - "All paths use ${CLAUDE_SKILL_DIR}/references/"
  artifacts:
    - path: "skills/insurance/SKILL.md"
      provides: "Full insurance advisory skill"
      min_lines: 300
  key_links:
    - from: "skills/insurance/SKILL.md"
      to: "skills/insurance/references/"
      via: "${CLAUDE_SKILL_DIR}/references/ paths"
      pattern: "CLAUDE_SKILL_DIR.*references"
    - from: "skills/insurance/SKILL.md"
      to: "skills/insurance/agents/"
      via: "Task tool agent delegation"
      pattern: "Task"
---

<objective>
Convert insurance.md into skills/insurance/SKILL.md following the validated conversion checklist.

Purpose: Health insurance advisory skill with calc and research agents
Output: Full SKILL.md replacing the stub
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-bulk-migration/16-CONTEXT.md
@.planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
@skills/tax/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert insurance command to insurance SKILL.md</name>
  <read_first>
    - commands/finyx/insurance.md
    - skills/insurance/SKILL.md (current stub)
    - skills/tax/SKILL.md (reference pattern)
    - .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
    - skills/insurance/references/ (list contents)
    - skills/insurance/agents/ (list contents)
  </read_first>
  <files>skills/insurance/SKILL.md</files>
  <action>
Overwrite the insurance SKILL.md stub with full skill content:

1. Frontmatter:
   - name: finyx-insurance
   - description: Under 250 chars, front-loaded. Start with "Health insurance advisor" — cover GKV vs PKV comparison, provider research, cost calculation, family coverage, switching analysis
   - allowed-tools: Copy from insurance.md. MUST include Task (has 2 agents)
   - disable-model-invocation: true (advisory skill)

2. Content: Direct conversion from insurance.md
   - Copy objective, execution_context, process, error_handling, notes sections
   - Update all paths to ${CLAUDE_SKILL_DIR}/references/
   - References available: disclaimer.md, germany/health-insurance.md

3. Agent delegation:
   - finyx-insurance-calc-agent.md — cost calculations
   - finyx-insurance-research-agent.md — provider research
   - Reference via Task tool as in source command

4. Path handling:
   - All reference paths: ${CLAUDE_SKILL_DIR}/references/
   - @.finyx/profile.json kept as-is
   - Zero @~/.claude/ occurrences
  </action>
  <verify>
    <automated>bash -c 'F="skills/insurance/SKILL.md"; grep -q "disable-model-invocation: true" "$F" && test $(grep -c "@~/.claude/" "$F") -eq 0 && test $(wc -l < "$F") -gt 300 && grep -q "finyx-insurance" "$F" && grep -q "Task" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - skills/insurance/SKILL.md > 300 lines
    - Contains `disable-model-invocation: true`
    - Contains `name: finyx-insurance`
    - Zero occurrences of `@~/.claude/`
    - Task in allowed-tools
    - Key domain terms: GKV, PKV, health insurance
  </acceptance_criteria>
  <done>insurance SKILL.md is a complete skill with agent delegation, proper frontmatter, portable paths</done>
</task>

<task type="auto">
  <name>Task 2: Validate insurance skill completeness</name>
  <read_first>
    - skills/insurance/SKILL.md (just written)
  </read_first>
  <files>skills/insurance/SKILL.md</files>
  <action>
Run validation checks per CONVERSION-CHECKLIST.md:

1. grep -q "disable-model-invocation: true" skills/insurance/SKILL.md
2. grep -c "@~/.claude/" (must be 0)
3. wc -l (must be > 300)
4. Verify ${CLAUDE_SKILL_DIR}/references/ paths resolve
5. Verify both agents exist in skills/insurance/agents/:
   - finyx-insurance-calc-agent.md
   - finyx-insurance-research-agent.md
6. Grep for key domain terms: "GKV", "PKV", "health", "insurance"
7. Verify Task in allowed-tools

Fix any issues found.
  </action>
  <verify>
    <automated>bash -c 'F="skills/insurance/SKILL.md"; grep -qi "GKV\|PKV" "$F" && grep -qi "health" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - All validation checks pass
    - Both agents exist
    - Key domain terms present
  </acceptance_criteria>
  <done>insurance skill validated against checklist</done>
</task>

</tasks>

<verification>
- skills/insurance/SKILL.md exists with > 300 lines
- disable-model-invocation: true present
- Zero @~/.claude/ paths
- 2 agents scoped under skills/insurance/agents/
- Task in allowed-tools
</verification>

<success_criteria>
insurance SKILL.md is a complete, validated skill with agent delegation, proper plugin frontmatter, and portable paths.
</success_criteria>

<output>
After completion, create `.planning/phases/16-bulk-migration/16-04-SUMMARY.md`
</output>
