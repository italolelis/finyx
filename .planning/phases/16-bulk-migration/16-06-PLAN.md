---
phase: 16-bulk-migration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/help/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-04, SKILL-05]

must_haves:
  truths:
    - "/finyx:help provides help, status checking, and update functionality"
    - "Skill does NOT have disable-model-invocation (utility skill, not advisory)"
    - "All paths use ${CLAUDE_SKILL_DIR}/references/ where applicable"
  artifacts:
    - path: "skills/help/SKILL.md"
      provides: "Full help + status + update utility skill"
      min_lines: 300
  key_links:
    - from: "skills/help/SKILL.md"
      to: ".finyx/"
      via: "Runtime file access for status"
      pattern: "\\.finyx/"
---

<objective>
Convert help.md + status.md + update.md into skills/help/SKILL.md following the validated conversion checklist.

Purpose: Merge help, status, and update utility commands into one skill
Output: Full SKILL.md replacing the stub
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-bulk-migration/16-CONTEXT.md
@.planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
@skills/tax/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert help + status + update commands to help SKILL.md</name>
  <read_first>
    - commands/finyx/help.md
    - commands/finyx/status.md
    - commands/finyx/update.md
    - skills/help/SKILL.md (current stub)
    - skills/tax/SKILL.md (reference pattern)
    - .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
  </read_first>
  <files>skills/help/SKILL.md</files>
  <action>
Overwrite the help SKILL.md stub with full skill content:

1. Frontmatter:
   - name: finyx-help
   - description: Under 250 chars, front-loaded. Start with "Finyx help and status" — cover command listing, project status, plugin updates, getting started guide
   - allowed-tools: Union of help.md + status.md + update.md allowed-tools. Remove Task if not needed
   - NO disable-model-invocation (utility skill, not advisory — similar rationale to realestate per D-07)

2. Content merge strategy:
   - help.md is the primary — its structure becomes the skeleton
   - Add a "Status" phase absorbing status.md content
   - Add an "Update" phase absorbing update.md content
   - Keep help.md's command listing and getting-started guide intact

3. Path handling:
   - Any reference paths: ${CLAUDE_SKILL_DIR}/references/
   - help skill has no reference docs (skills/help/references/ is empty) — only runtime file access
   - @.finyx/profile.json and @.immo/ paths kept as-is
   - Zero @~/.claude/ occurrences
  </action>
  <verify>
    <automated>bash -c 'F="skills/help/SKILL.md"; test ! -z "$(grep "disable-model-invocation" "$F")" && echo "FAIL: has disable-model-invocation" && exit 1; test $(grep -c "@~/.claude/" "$F") -eq 0 && test $(wc -l < "$F") -gt 300 && grep -q "finyx-help" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - skills/help/SKILL.md > 300 lines
    - Does NOT contain `disable-model-invocation: true`
    - Contains `name: finyx-help`
    - Zero occurrences of `@~/.claude/`
    - Contains status and update content (grep for "status" and "update")
  </acceptance_criteria>
  <done>help SKILL.md is a complete utility skill with help + status + update merged, proper frontmatter, portable paths</done>
</task>

<task type="auto">
  <name>Task 2: Validate help skill completeness</name>
  <read_first>
    - skills/help/SKILL.md (just written)
  </read_first>
  <files>skills/help/SKILL.md</files>
  <action>
Run validation checks per CONVERSION-CHECKLIST.md:

1. Verify NO disable-model-invocation in frontmatter
2. grep -c "@~/.claude/" (must be 0)
3. wc -l (must be > 300)
4. Grep for key terms from each merged command:
   - help: "command" or "available" or "getting started"
   - status: "status" or "progress" or "STATE"
   - update: "update" or "version"

Fix any issues found.
  </action>
  <verify>
    <automated>bash -c 'F="skills/help/SKILL.md"; grep -qi "status" "$F" && grep -qi "update\|version" "$F" && grep -qi "command\|help" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - All validation checks pass
    - All 3 merged command domains represented
  </acceptance_criteria>
  <done>help skill validated against checklist</done>
</task>

</tasks>

<verification>
- skills/help/SKILL.md exists with > 300 lines
- No disable-model-invocation
- Zero @~/.claude/ paths
- All 3 command domains merged
</verification>

<success_criteria>
help SKILL.md is a complete utility skill combining help + status + update with proper plugin frontmatter and portable paths.
</success_criteria>

<output>
After completion, create `.planning/phases/16-bulk-migration/16-06-SUMMARY.md`
</output>
