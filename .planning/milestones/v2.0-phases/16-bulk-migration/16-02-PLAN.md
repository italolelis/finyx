---
phase: 16-bulk-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/realestate/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-03, SKILL-04, SKILL-05]

must_haves:
  truths:
    - "/finyx:realestate provides access to all 7 real estate workflows (scout, analyze, filter, compare, stress-test, report, rates)"
    - "Skill does NOT have disable-model-invocation (per D-07)"
    - "All 3 agents scoped under skills/realestate/agents/"
    - "All paths use ${CLAUDE_SKILL_DIR}/references/"
  artifacts:
    - path: "skills/realestate/SKILL.md"
      provides: "Full real estate skill with 7 merged command workflows"
      min_lines: 800
  key_links:
    - from: "skills/realestate/SKILL.md"
      to: "skills/realestate/references/"
      via: "${CLAUDE_SKILL_DIR}/references/ paths"
      pattern: "CLAUDE_SKILL_DIR.*references"
    - from: "skills/realestate/SKILL.md"
      to: "skills/realestate/agents/"
      via: "Task tool agent delegation"
      pattern: "Task"
---

<objective>
Convert 7 real estate commands (scout, analyze, filter, compare, stress-test, report, rates) into skills/realestate/SKILL.md.

Purpose: Merge all real estate workflows into one orchestrating skill
Output: Full SKILL.md acting as router/orchestrator for real estate workflows
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-bulk-migration/16-CONTEXT.md
@.planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
@skills/tax/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert 7 real estate commands to realestate SKILL.md</name>
  <read_first>
    - commands/finyx/scout.md
    - commands/finyx/analyze.md
    - commands/finyx/filter.md
    - commands/finyx/compare.md
    - commands/finyx/stress-test.md
    - commands/finyx/report.md
    - commands/finyx/rates.md
    - skills/realestate/SKILL.md (current stub)
    - skills/tax/SKILL.md (reference pattern)
    - .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
    - skills/realestate/references/ (list contents)
    - skills/realestate/agents/ (list contents)
  </read_first>
  <files>skills/realestate/SKILL.md</files>
  <action>
Overwrite the realestate SKILL.md stub with full skill content:

1. Frontmatter:
   - name: finyx-realestate
   - description: Under 250 chars, front-loaded. Start with "Real estate investment analysis" — cover location scouting, property analysis, filtering, comparison, stress testing, report generation, mortgage rates
   - allowed-tools: Union of all 7 source commands' allowed-tools. MUST include Task (has 3 agents: analyzer, location-scout, reporter)
   - NO disable-model-invocation (per D-07 — realestate is action-based, not advisory)

2. Structure as router/orchestrator:
   - The SKILL.md acts as a unified entry point with the user specifying which workflow they want
   - Objective section explains this is a multi-workflow skill
   - Each original command becomes a major phase/section:
     * Phase: Location Scouting (from scout.md)
     * Phase: Property Analysis (from analyze.md)
     * Phase: Filtering (from filter.md)
     * Phase: Comparison (from compare.md)
     * Phase: Stress Testing (from stress-test.md)
     * Phase: Report Generation (from report.md)
     * Phase: Rate Checking (from rates.md)
   - Include a routing section at the top that determines which workflow to execute based on user intent

3. Path handling:
   - All reference paths: ${CLAUDE_SKILL_DIR}/references/
   - References available: disclaimer.md, methodology.md, erbpacht-detection.md, transport-assessment.md, germany/tax-rules.md
   - @.finyx/profile.json and @.immo/ paths kept as-is (runtime files)
   - Zero @~/.claude/ occurrences

4. Agent delegation:
   - finyx-analyzer-agent.md — used by analyze workflow
   - finyx-location-scout.md — used by scout workflow
   - finyx-reporter-agent.md — used by report workflow
   - Reference agents via Task tool as in source commands
  </action>
  <verify>
    <automated>bash -c 'F="skills/realestate/SKILL.md"; test ! -z "$(grep "disable-model-invocation" "$F")" && echo "FAIL: has disable-model-invocation" && exit 1; test $(grep -c "@~/.claude/" "$F") -eq 0 && test $(wc -l < "$F") -gt 800 && grep -q "finyx-realestate" "$F" && grep -q "Task" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - skills/realestate/SKILL.md > 800 lines
    - Does NOT contain `disable-model-invocation: true`
    - Contains `name: finyx-realestate`
    - Zero occurrences of `@~/.claude/`
    - Contains Task in allowed-tools
    - All 7 workflow phases present (grep for scout, analyze, filter, compare, stress, report, rate)
  </acceptance_criteria>
  <done>realestate SKILL.md is a complete orchestrating skill with all 7 real estate workflows, proper frontmatter, agent delegation, portable paths</done>
</task>

<task type="auto">
  <name>Task 2: Validate realestate skill completeness</name>
  <read_first>
    - skills/realestate/SKILL.md (just written)
  </read_first>
  <files>skills/realestate/SKILL.md</files>
  <action>
Run validation checks per CONVERSION-CHECKLIST.md:

1. Verify NO disable-model-invocation in frontmatter (per D-07)
2. grep -c "@~/.claude/" (must be 0)
3. wc -l (must be > 800)
4. Verify all ${CLAUDE_SKILL_DIR}/references/ paths resolve to files in skills/realestate/references/
5. Verify all 3 agents exist in skills/realestate/agents/:
   - finyx-analyzer-agent.md
   - finyx-location-scout.md
   - finyx-reporter-agent.md
6. Grep for key domain terms from each workflow:
   - scout: "location", "transport"
   - analyze: "yield", "Kaufnebenkosten" or "closing cost"
   - filter: "ranking", "exclusion"
   - compare: "shortlist"
   - stress-test: "vacancy", "interest"
   - report: "briefing"
   - rates: "mortgage", "Zinsen" or "rate"
7. Verify Task is in allowed-tools

Fix any issues found.
  </action>
  <verify>
    <automated>bash -c 'F="skills/realestate/SKILL.md"; for term in location yield ranking shortlist vacancy briefing mortgage; do grep -qi "$term" "$F" || { echo "FAIL: missing $term"; exit 1; }; done; echo "PASS"'</automated>
  </verify>
  <acceptance_criteria>
    - All 7 workflow domain terms present
    - All 3 agents exist under skills/realestate/agents/
    - No broken reference paths
  </acceptance_criteria>
  <done>realestate skill validated — all 7 workflows present, agents scoped, paths portable</done>
</task>

</tasks>

<verification>
- skills/realestate/SKILL.md exists with > 800 lines
- No disable-model-invocation (per D-07)
- Zero @~/.claude/ paths
- All 7 command workflows represented
- 3 agents scoped under skills/realestate/agents/
- Task in allowed-tools
</verification>

<success_criteria>
realestate SKILL.md is a complete orchestrating skill combining all 7 real estate workflows with proper routing, agent delegation, and portable paths.
</success_criteria>

<output>
After completion, create `.planning/phases/16-bulk-migration/16-02-SUMMARY.md`
</output>
