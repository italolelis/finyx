---
phase: 16-bulk-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/insights/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, SKILL-04, SKILL-05]

must_haves:
  truths:
    - "/finyx:insights produces cross-domain financial insights dashboard"
    - "Skill has disable-model-invocation: true"
    - "Both agents scoped under skills/insights/agents/"
    - "All paths use ${CLAUDE_SKILL_DIR}/references/"
  artifacts:
    - path: "skills/insights/SKILL.md"
      provides: "Full insights advisory skill"
      min_lines: 200
  key_links:
    - from: "skills/insights/SKILL.md"
      to: "skills/insights/references/"
      via: "${CLAUDE_SKILL_DIR}/references/ paths"
      pattern: "CLAUDE_SKILL_DIR.*references"
    - from: "skills/insights/SKILL.md"
      to: "skills/insights/agents/"
      via: "Task tool agent delegation"
      pattern: "Task"
---

<objective>
Convert insights.md into skills/insights/SKILL.md following the validated conversion checklist.

Purpose: Financial insights dashboard skill with allocation and projection agents
Output: Full SKILL.md replacing the stub
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-bulk-migration/16-CONTEXT.md
@.planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
@skills/tax/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert insights command to insights SKILL.md</name>
  <read_first>
    - commands/finyx/insights.md
    - skills/insights/SKILL.md (current stub)
    - skills/tax/SKILL.md (reference pattern)
    - .planning/phases/15-pilot-skill/CONVERSION-CHECKLIST.md
    - skills/insights/references/ (list contents)
    - skills/insights/agents/ (list contents)
  </read_first>
  <files>skills/insights/SKILL.md</files>
  <action>
Overwrite the insights SKILL.md stub with full skill content:

1. Frontmatter:
   - name: finyx-insights
   - description: Under 250 chars, front-loaded. Start with "Financial insights dashboard" — cover portfolio scoring, asset allocation analysis, wealth projection, cross-domain benchmarking
   - allowed-tools: Copy from insights.md. MUST include Task (has 2 agents)
   - disable-model-invocation: true (advisory skill)

2. Content: Direct conversion from insights.md
   - Copy objective, execution_context, process, error_handling, notes sections
   - Update all paths to ${CLAUDE_SKILL_DIR}/references/
   - References available: disclaimer.md, insights/benchmarks.md, insights/scoring-rules.md

3. Agent delegation:
   - finyx-allocation-agent.md — asset allocation analysis
   - finyx-projection-agent.md — wealth projection
   - Reference via Task tool as in source command

4. Path handling:
   - All reference paths: ${CLAUDE_SKILL_DIR}/references/
   - @.finyx/profile.json kept as-is
   - Zero @~/.claude/ occurrences

5. Cross-skill references: insights reads data from tax, invest, insurance skills — add fallback notes for any cross-skill data dependencies
  </action>
  <verify>
    <automated>bash -c 'F="skills/insights/SKILL.md"; grep -q "disable-model-invocation: true" "$F" && test $(grep -c "@~/.claude/" "$F") -eq 0 && test $(wc -l < "$F") -gt 200 && grep -q "finyx-insights" "$F" && grep -q "Task" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - skills/insights/SKILL.md > 200 lines
    - Contains `disable-model-invocation: true`
    - Contains `name: finyx-insights`
    - Zero occurrences of `@~/.claude/`
    - Task in allowed-tools
    - Key domain terms: scoring, allocation, projection, benchmark
  </acceptance_criteria>
  <done>insights SKILL.md is a complete skill with agent delegation, proper frontmatter, portable paths</done>
</task>

<task type="auto">
  <name>Task 2: Validate insights skill completeness</name>
  <read_first>
    - skills/insights/SKILL.md (just written)
  </read_first>
  <files>skills/insights/SKILL.md</files>
  <action>
Run validation checks per CONVERSION-CHECKLIST.md:

1. grep -q "disable-model-invocation: true" skills/insights/SKILL.md
2. grep -c "@~/.claude/" (must be 0)
3. wc -l (must be > 200)
4. Verify ${CLAUDE_SKILL_DIR}/references/ paths resolve
5. Verify both agents exist in skills/insights/agents/:
   - finyx-allocation-agent.md
   - finyx-projection-agent.md
6. Grep for key domain terms: "scoring", "allocation", "projection", "benchmark"
7. Verify Task in allowed-tools

Fix any issues found.
  </action>
  <verify>
    <automated>bash -c 'F="skills/insights/SKILL.md"; grep -qi "scoring\|score" "$F" && grep -qi "allocation\|allocat" "$F" && grep -qi "projection\|project" "$F" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - All validation checks pass
    - Both agents exist
    - Key domain terms present
  </acceptance_criteria>
  <done>insights skill validated against checklist</done>
</task>

</tasks>

<verification>
- skills/insights/SKILL.md exists with > 200 lines
- disable-model-invocation: true present
- Zero @~/.claude/ paths
- 2 agents scoped under skills/insights/agents/
- Task in allowed-tools
</verification>

<success_criteria>
insights SKILL.md is a complete, validated skill with agent delegation, proper plugin frontmatter, and portable paths.
</success_criteria>

<output>
After completion, create `.planning/phases/16-bulk-migration/16-05-SUMMARY.md`
</output>
