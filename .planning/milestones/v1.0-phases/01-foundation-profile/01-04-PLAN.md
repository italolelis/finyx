---
phase: 01-foundation-profile
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/finyx/help.md
  - agents/finyx-analyzer-agent.md
  - agents/finyx-reporter-agent.md
autonomous: true
gap_closure: true
requirements:
  - FOUND-03
  - FOUND-05
  - PROF-05

must_haves:
  truths:
    - "All 11 finyx commands include disclaimer.md in execution_context"
    - "All agent files reference .finyx/profile.json as the profile path"
  artifacts:
    - path: "commands/finyx/help.md"
      provides: "execution_context block with disclaimer include"
      contains: "disclaimer.md"
    - path: "agents/finyx-analyzer-agent.md"
      provides: "Correct profile path in Input Context"
      contains: ".finyx/profile.json"
    - path: "agents/finyx-reporter-agent.md"
      provides: "Correct profile path in Input Context"
      contains: ".finyx/profile.json"
  key_links:
    - from: "commands/finyx/help.md"
      to: "finyx/references/disclaimer.md"
      via: "@path include in execution_context"
      pattern: "@~/.claude/finyx/references/disclaimer.md"
    - from: "agents/finyx-analyzer-agent.md"
      to: ".finyx/profile.json"
      via: "documented input context path"
      pattern: "\\.finyx/profile\\.json"
    - from: "agents/finyx-reporter-agent.md"
      to: ".finyx/profile.json"
      via: "documented input context path"
      pattern: "\\.finyx/profile\\.json"
---

<objective>
Close two verification gaps from Phase 1: (1) add missing execution_context with disclaimer to help.md, (2) fix stale .finyx/config.json references in agent files to .finyx/profile.json.

Purpose: Satisfy FOUND-03, FOUND-05, and PROF-05 which are currently PARTIAL due to these gaps.
Output: Three corrected files passing all verification checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-profile/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add execution_context with disclaimer to help.md</name>
  <files>commands/finyx/help.md</files>
  <read_first>
    - commands/finyx/help.md
    - commands/finyx/analyze.md (reference for execution_context pattern)
  </read_first>
  <action>
Insert an `<execution_context>` block into `commands/finyx/help.md` immediately after the closing `</objective>` tag (after line 13) and before the `<reference>` tag. The block must contain:

```
<execution_context>
@~/.claude/finyx/references/disclaimer.md
</execution_context>
```

This matches the pattern used in all other finyx commands (e.g., analyze.md, scout.md). Do NOT modify any other content in the file.

Addresses verification Gap 1: "help.md has no execution_context block — disclaimer.md not included" (FOUND-05, PROF-05).
  </action>
  <verify>
    <automated>grep -c "disclaimer.md" commands/finyx/help.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@~/.claude/finyx/references/disclaimer.md" commands/finyx/help.md` returns a match
    - `grep -c "<execution_context>" commands/finyx/help.md` returns 1
    - All 11 commands now include disclaimer: `for f in commands/finyx/*.md; do grep -l "disclaimer.md" "$f"; done | wc -l` returns 11
  </acceptance_criteria>
  <done>help.md contains execution_context block with disclaimer.md include, matching the pattern of all other finyx commands</done>
</task>

<task type="auto">
  <name>Task 2: Fix agent profile path references from config.json to profile.json</name>
  <files>agents/finyx-analyzer-agent.md, agents/finyx-reporter-agent.md</files>
  <read_first>
    - agents/finyx-analyzer-agent.md
    - agents/finyx-reporter-agent.md
  </read_first>
  <action>
In `agents/finyx-analyzer-agent.md`, find line 13 which reads:
```
- Investor config from `.finyx/config.json`
```
Replace with:
```
- Investor profile from `.finyx/profile.json`
```

In `agents/finyx-reporter-agent.md`, find lines 12-13 which read:
```
- Investor profile from `.finyx/config.json`
```
Replace with:
```
- Investor profile from `.finyx/profile.json`
```

These are the ONLY changes needed. Do NOT modify any other content in either file. The canonical profile path is `.finyx/profile.json` as established by `commands/finyx/profile.md` and the profile.json template.

Addresses verification Gap 2: "finyx-analyzer-agent.md and finyx-reporter-agent.md document their input as .finyx/config.json (stale path)" (FOUND-03).
  </action>
  <verify>
    <automated>grep -rn "config\.json" agents/finyx-analyzer-agent.md agents/finyx-reporter-agent.md | grep -v "^$" && echo "FAIL: stale config.json refs remain" || echo "PASS: no config.json refs"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "profile.json" agents/finyx-analyzer-agent.md` returns a match containing `.finyx/profile.json`
    - `grep "profile.json" agents/finyx-reporter-agent.md` returns a match containing `.finyx/profile.json`
    - `grep "config.json" agents/finyx-analyzer-agent.md agents/finyx-reporter-agent.md` returns zero matches
  </acceptance_criteria>
  <done>Both agent files reference .finyx/profile.json as the investor profile source, matching the canonical path used by all commands</done>
</task>

</tasks>

<verification>
All three verification gap conditions resolved:
1. `grep -c "disclaimer.md" commands/finyx/help.md` returns 1
2. `grep -c "profile.json" agents/finyx-analyzer-agent.md` returns >= 1 (with no config.json matches)
3. `grep -c "profile.json" agents/finyx-reporter-agent.md` returns >= 1 (with no config.json matches)
4. Full sweep: `grep -rn "\.finyx/config\.json" commands/ agents/` returns zero results
</verification>

<success_criteria>
- All 11 finyx commands include disclaimer.md in execution_context
- All agent files reference .finyx/profile.json (zero references to .finyx/config.json)
- FOUND-03, FOUND-05, PROF-05 move from PARTIAL to SATISFIED
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-profile/01-04-SUMMARY.md`
</output>
