---
phase: 12-cross-advisor-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - finyx/templates/config.json
  - commands/finyx/insights.md
  - commands/finyx/tax.md
autonomous: true
requirements:
  - EDGE-03

must_haves:
  truths:
    - "profile.json template has an insurance section with type, monthly_cost, employer_share, provider fields"
    - "/finyx:insights picks up insurance cost in allocation analysis when insurance section is populated"
    - "/finyx:tax includes PKV Basisabsicherung deduction section when insurance.type is PKV"
    - "Both commands skip insurance sections silently when insurance section is absent or empty"
  artifacts:
    - path: "finyx/templates/config.json"
      provides: "Insurance section in profile schema"
      contains: "insurance"
    - path: "commands/finyx/insights.md"
      provides: "Insurance cost pickup in allocation analysis"
      contains: "insurance"
    - path: "commands/finyx/tax.md"
      provides: "PKV Basisabsicherung deduction section"
      contains: "Basisabsicherung"
  key_links:
    - from: "commands/finyx/insights.md"
      to: ".finyx/profile.json"
      via: "reads insurance.monthly_cost for allocation"
      pattern: "insurance\\.monthly_cost"
    - from: "commands/finyx/tax.md"
      to: "finyx/references/germany/health-insurance.md"
      via: "references Section 5 caps for PKV deduction"
      pattern: "§10 EStG|Basisabsicherung"
---

<objective>
Wire insurance data into the cross-advisor financial picture. Extend the profile template with an insurance section, make /finyx:insights include insurance costs in allocation analysis, and make /finyx:tax show PKV Basisabsicherung deduction when applicable.

Purpose: Completes EDGE-03 — insurance costs feed into existing advisors so the full financial picture reflects health insurance decisions.
Output: Three modified files — profile template, insights command, tax command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cross-advisor-integration/12-CONTEXT.md

@finyx/templates/config.json
@commands/finyx/insights.md
@commands/finyx/tax.md
@finyx/references/germany/health-insurance.md

<interfaces>
<!-- Profile template — current top-level sections -->
From finyx/templates/config.json:
Top-level keys: project, investor, strategy, criteria, assumptions
New "insurance" section goes at same level as these.

<!-- Insights command — key integration points -->
From commands/finyx/insights.md:
- Phase 1: Validation and Completeness Gate (check required fields)
- Phase 3: Agent Spawning (allocation agent reads profile)
- Phase 5: Report Synthesis — Section 1 Summary (allocation overview)
- Cross-advisor links section (CAL-01 through CAL-04 patterns)

<!-- Tax command — key integration points -->
From commands/finyx/tax.md:
- Phase 1: Validation (reads profile, determines active countries)
- Phase 3: German Investment Tax (subsections 3.1–3.5)
- Phase 6: Disclaimer (appended at end)

<!-- Health insurance reference — Section 5 deduction caps -->
From finyx/references/germany/health-insurance.md:
- §10 EStG employee cap: €1,900/year
- §10 EStG self-employed cap: €2,800/year
- Basisabsicherung portion estimate: PKV monthly × 0.85
- Tax benefit formula: min(basisabsicherung × 12, cap) × marginal_rate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add insurance section to profile template</name>
  <files>finyx/templates/config.json</files>
  <read_first>finyx/templates/config.json</read_first>
  <action>
Add an `insurance` section to `finyx/templates/config.json` at the same level as `investor`, `strategy`, `criteria`, `assumptions` (per D-01).

The insurance section must contain exactly these fields:
```json
"insurance": {
  "type": null,
  "monthly_cost": null,
  "employer_share": null,
  "provider": null
}
```

Field semantics:
- `type`: string — "GKV", "PKV", or "none". Null means not yet configured.
- `monthly_cost`: number — total monthly premium in EUR. Null means not configured.
- `employer_share`: number — employer contribution in EUR per month. Null means not configured.
- `provider`: string (optional) — insurance provider name. Null means not set.

Place the insurance section AFTER the `investor` block and BEFORE the `strategy` block. This groups personal financial data together.

Do NOT modify any other section of config.json. Do NOT change the version number.
  </action>
  <verify>
    <automated>grep -q '"insurance"' finyx/templates/config.json && grep -q '"type": null' finyx/templates/config.json && grep -q '"monthly_cost": null' finyx/templates/config.json && grep -q '"employer_share": null' finyx/templates/config.json && grep -q '"provider": null' finyx/templates/config.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- finyx/templates/config.json contains `"insurance"` top-level key
- insurance section has `type`, `monthly_cost`, `employer_share`, `provider` fields all set to null
- Existing sections (project, investor, strategy, criteria, assumptions) unchanged
- JSON is valid (no syntax errors)
  </acceptance_criteria>
  <done>Profile template has insurance section with all four fields at null defaults</done>
</task>

<task type="auto">
  <name>Task 2: Add insurance cost pickup to /finyx:insights</name>
  <files>commands/finyx/insights.md</files>
  <read_first>commands/finyx/insights.md</read_first>
  <action>
Make three targeted additions to `commands/finyx/insights.md` (per D-02):

**Addition 1 — Phase 1 completeness gate:** After the existing "Required fields — Allocation agent" block, add a note:

```
**Optional fields — Insurance integration:**
- `insurance.type` and `insurance.monthly_cost` — if populated, insurance costs are included in allocation analysis as a "needs" line item. If absent or null: skip silently (no error, no warning).
```

This is NOT a required field — it does not block report generation. It follows the existing completeness gate pattern where absent sections are skipped.

**Addition 2 — Phase 3 allocation agent prompt:** In the Task 1 allocation agent spawn prompt, add this paragraph after the existing profile data instruction:

```
If `.finyx/profile.json` contains an `insurance` section with `type` and `monthly_cost` populated:
- Include the net insurance cost (monthly_cost minus employer_share) as a "needs" category line item labeled "Health Insurance ([type])"
- If insurance.type is "PKV" and the net cost exceeds what GKV would cost at the user's income level, flag this as a recommendation opportunity: "PKV premium exceeds estimated GKV equivalent — review /finyx:insurance for cost comparison"
- If insurance section is absent, null, or type is "none": skip — do not include any insurance line in allocation
```

**Addition 3 — Cross-advisor links:** After the existing CAL-04 pattern block in Phase 5 Section 3, add a new pattern:

```
**Pattern CAL-05: High Insurance Cost — PKV Premium Drag on Savings Rate**
- Trigger: `insurance.type == "PKV"` AND net insurance cost (monthly_cost - employer_share) exceeds €400/month AND savings rate is below 20%
- Insight: High PKV premiums reduce disposable income available for savings and investments. The user may benefit from reviewing PKV tariff optimization (Tarifwechsel §204 VVG) or considering GKV return if still eligible.
- Recommendation: "Review PKV tariff options via /finyx:insurance — Tarifwechsel within same insurer can reduce premiums without losing Altersrückstellungen"
- Estimated impact: Potential monthly saving from tariff optimization (varies, typically €50–200/month = €600–2,400/year)
```

Do NOT modify any other part of insights.md. Preserve all existing content exactly.
  </action>
  <verify>
    <automated>grep -q "insurance.type" commands/finyx/insights.md && grep -q "insurance.monthly_cost" commands/finyx/insights.md && grep -q "CAL-05" commands/finyx/insights.md && grep -q "Health Insurance" commands/finyx/insights.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- commands/finyx/insights.md contains "insurance.type" reference
- commands/finyx/insights.md contains "insurance.monthly_cost" reference
- commands/finyx/insights.md contains CAL-05 cross-advisor pattern
- commands/finyx/insights.md contains "Health Insurance" allocation line item instruction
- Existing CAL-01 through CAL-04 patterns preserved
- Phase 1 completeness gate marks insurance as optional, not required
  </acceptance_criteria>
  <done>Insights command picks up insurance costs in allocation and has CAL-05 cross-advisor pattern</done>
</task>

<task type="auto">
  <name>Task 3: Add PKV Basisabsicherung deduction section to /finyx:tax</name>
  <files>commands/finyx/tax.md</files>
  <read_first>commands/finyx/tax.md, finyx/references/germany/health-insurance.md</read_first>
  <action>
Add a new subsection to `commands/finyx/tax.md` Phase 3 (German Investment Tax) for PKV deduction (per D-03). This section is CONDITIONAL — only shown when insurance.type is PKV.

**Addition — New Phase 3.6 after existing Phase 3.5 (Vorabpauschale):**

Add this section:

```markdown
### 3.6 PKV Basisabsicherung Deduction (§10 EStG)

*Execute this subsection only if `insurance.type == "PKV"` in `.finyx/profile.json`. If insurance section is absent, null, or type is not "PKV": skip entirely.*

Using health-insurance.md Section 5 (loaded via `@~/.claude/finyx/references/germany/health-insurance.md`), show the PKV tax deduction calculation:

**Read from profile:**
- `insurance.monthly_cost` — total PKV monthly premium
- `insurance.employer_share` — employer contribution (0 if self-employed)
- `countries.germany.marginal_rate` — marginal tax rate

**Determine cap:**
- If employer_share > 0: employee cap = €1,900/year
- If employer_share == 0: self-employed cap = €2,800/year

**Calculate and display:**

` ` `
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PKV BASISABSICHERUNG DEDUCTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PKV monthly premium:                [monthly_cost] EUR
Employer share:                     [employer_share] EUR
Net monthly cost:                   [monthly_cost - employer_share] EUR

Basisabsicherung estimate (85%):    [net × 0.85] EUR/month
Annual deductible:                  min([basis × 12], [cap]) = [annual_deductible] EUR
Deduction cap ([employee/self-employed]):  [1,900 or 2,800] EUR

Tax benefit at [marginal_rate]%:    [annual_deductible × marginal_rate] EUR/year
Monthly tax saving:                 [tax_benefit / 12] EUR/month
Effective net PKV cost:             [monthly_cost - employer_share - (tax_benefit/12)] EUR/month
` ` `

**Key notes to include:**
- The Basisabsicherung portion (typically 80–90% of premium) covers only basic health coverage — not Zusatzleistungen, Krankentagegeld, or Zahnzusatz
- The §10 EStG cap of €1,900/€2,800 is usually reached quickly for PKV premiums above €200/month
- From 2026, insurers transmit Basisabsicherung data via ELStAM for employees — self-employed still file via Anlage Vorsorgeaufwand
- This deduction is separate from investment tax — it reduces total taxable income, indirectly benefiting all income types

**Cross-reference:**
> For full PKV vs GKV cost comparison and long-term projections, run `/finyx:insurance`.
```

Also add `@~/.claude/finyx/references/germany/health-insurance.md` to the `<execution_context>` block so the tax command has access to the deduction caps at runtime.

Do NOT modify any other phase or subsection. Preserve all existing content.

IMPORTANT: The triple backticks in the banner display above are escaped with spaces — use actual triple backticks in the output (no spaces between them).
  </action>
  <verify>
    <automated>grep -q "3.6 PKV Basisabsicherung" commands/finyx/tax.md && grep -q "insurance.type" commands/finyx/tax.md && grep -q "health-insurance.md" commands/finyx/tax.md && grep -q "§10 EStG" commands/finyx/tax.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- commands/finyx/tax.md contains "### 3.6 PKV Basisabsicherung" subsection
- Section is conditional on `insurance.type == "PKV"`
- Section references §10 EStG caps (€1,900 employee, €2,800 self-employed)
- Section includes calculation formula using marginal_rate from profile
- health-insurance.md is in execution_context block
- Existing phases 3.1–3.5, 4, 5, 6 unchanged
  </acceptance_criteria>
  <done>Tax command shows PKV Basisabsicherung deduction when insurance.type is PKV, skips otherwise</done>
</task>

</tasks>

<verification>
All three success criteria from ROADMAP Phase 12:

1. Profile schema extended: `grep "insurance" finyx/templates/config.json` shows insurance section with type, monthly_cost, employer_share, provider
2. Insights picks up insurance: `grep -c "insurance" commands/finyx/insights.md` returns 3+ matches (completeness gate, agent prompt, CAL-05)
3. Tax reflects PKV deduction: `grep "Basisabsicherung" commands/finyx/tax.md` shows Section 3.6 with conditional PKV deduction

Cross-check: both commands skip insurance section silently when absent (grep for "skip" or "absent" in both files).
</verification>

<success_criteria>
- EDGE-03 fully satisfied: insurance costs feed into /finyx:insights and /finyx:tax via profile schema extension
- Profile template has insurance section with 4 fields (type, monthly_cost, employer_share, provider)
- Insights command includes insurance in allocation as "needs" line item when populated
- Tax command shows PKV Basisabsicherung §10 EStG deduction calculation when insurance.type is PKV
- Both commands gracefully skip insurance when section is absent or null
</success_criteria>

<output>
After completion, create `.planning/phases/12-cross-advisor-integration/12-01-SUMMARY.md`
</output>
